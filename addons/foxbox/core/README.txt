tate_lib/modules/core

Very rudimentary and abstract classes that are very generic and used elsewhere.

The best comparison for this would be something like a math library.
For example, one component in this module, "bounded_value", literally just
provides a basic resource that keeps a value clamped and does some signals
depending on what happens to it. 
